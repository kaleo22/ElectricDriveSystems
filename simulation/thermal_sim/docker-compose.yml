services:
  thermal-dev:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: thermal-simulation-dev

    volumes:
      - .:/workspace

    working_dir: /workspace

    tty: true
    stdin_open: true

    command: bash